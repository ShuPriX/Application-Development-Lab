<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <style>
        .hidden { display: none; }
        .active-menu { background: var(--accent); color: white; }
        
        /* File Upload Styles */
        .file-group { margin-bottom: 15px; border: 1px solid var(--border); padding: 15px; border-radius: 8px; }
        .file-label { display: block; margin-bottom: 5px; font-weight: bold; font-size: 0.9em; }
        .current-file { display: inline-block; margin-top: 5px; font-size: 0.85em; color: var(--accent); text-decoration: none; }
        .current-file:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <div class="dashboard">
        <div class="sidebar">
            <h2 style="color: var(--accent); margin-bottom: 40px;">StudentPortal</h2>
            <div id="btn-dashboard" class="menu-item active-menu" onclick="showSection('dashboard')">
                <ion-icon name="grid-outline"></ion-icon> Dashboard
            </div>
            <div id="btn-profile" class="menu-item" onclick="showSection('profile')">
                <ion-icon name="person-outline"></ion-icon> Profile
            </div>
            <div style="margin-top: auto;">
                <button id="theme-toggle" class="theme-switch">🌙 Dark Mode</button>
                <a href="/logout" class="menu-item" style="color: #ff4444; margin-top: 10px;">
                    <ion-icon name="log-out-outline"></ion-icon> Logout
                </a>
            </div>
        </div>

        <div class="main">
            <div class="banner">
                <div>
                    <h1>Hello, <%= user.first_name %>!</h1>
                    <p>Welcome to your student portal.</p>
                </div>
                <div style="font-size: 60px;">🎓</div>
            </div>

            <div id="section-dashboard">
                <h3>Academic Results</h3>
                <div class="card-grid">
                    <% if (grades.length > 0) { %>
                        <% grades.forEach(function(grade) { %>
                        <div class="card" style="display: flex; justify-content: space-between; align-items: center;">
                            <div>
                                <h4 style="margin: 0;"><%= grade.subject %></h4>
                                <p style="color: var(--text-secondary); font-size: 0.9em; margin-top: 5px;">Score: <%= grade.score %></p>
                            </div>
                            <div style="background: rgba(172, 88, 233, 0.1); color: var(--accent); padding: 15px; border-radius: 10px; font-weight: bold; font-size: 1.2em;">
                                <%= grade.grade %>
                            </div>
                        </div>
                        <% }); %>
                    <% } else { %>
                        <p>No grades uploaded yet.</p>
                    <% } %>
                </div>
            </div>

            <div id="section-profile" class="hidden">
                <div style="display: flex; gap: 20px; flex-wrap: wrap;">
                    
                    <div style="flex: 1; min-width: 300px;">
                        <h3>Edit Details & Documents</h3>
                        <div class="card">
                            <form action="/update-profile" method="POST" enctype="multipart/form-data">
                                
                                <label>Username</label>
                                <input type="text" name="username" value="<%= user.username %>" required>
                                
                                <label>Email</label>
                                <input type="email" name="email" value="<%= user.email %>" required>

                                <hr style="border: 0; border-top: 1px solid var(--border); margin: 20px 0;">
                                
                                <div class="file-group">
                                    <span class="file-label">Resume (PDF/Doc)</span>
                                    <input type="file" name="resume" style="border:none; padding-left:0;">
                                    <% if (user.resume) { %>
                                        <br>✅ <a href="<%= user.resume %>" target="_blank" class="current-file">View Current Resume</a>
                                    <% } %>
                                </div>

                                <div class="file-group">
                                    <span class="file-label">Cover Letter (PDF/Doc)</span>
                                    <input type="file" name="cover_letter" style="border:none; padding-left:0;">
                                    <% if (user.cover_letter) { %>
                                        <br>✅ <a href="<%= user.cover_letter %>" target="_blank" class="current-file">View Current Cover Letter</a>
                                    <% } %>
                                </div>

                                <button type="submit" class="primary">Save Changes</button>
                            </form>
                        </div>
                    </div>

                    <div style="flex: 1; min-width: 300px;">
                        <h3>Security</h3>
                        <div class="card">
                            <form action="/reset-password" method="POST">
                                <label>Change Password</label>
                                <input type="password" name="new_password" placeholder="New Password" required>
                                <button type="submit" class="primary" style="background: #ff4444;">Reset Password</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function showSection(sectionName) {
            document.getElementById('section-dashboard').classList.add('hidden');
            document.getElementById('section-profile').classList.add('hidden');
            document.getElementById('btn-dashboard').classList.remove('active-menu');
            document.getElementById('btn-profile').classList.remove('active-menu');

            document.getElementById('section-' + sectionName).classList.remove('hidden');
            document.getElementById('btn-' + sectionName).classList.add('active-menu');
        }

        const toggleBtn = document.getElementById('theme-toggle');
        const root = document.documentElement;
        if(localStorage.getItem('theme') === 'dark') {
            root.setAttribute('data-theme', 'dark');
            toggleBtn.innerText = '☀️ Light Mode';
        }

        toggleBtn.addEventListener('click', () => {
            if (root.getAttribute('data-theme') === 'dark') {
                root.setAttribute('data-theme', 'light');
                localStorage.setItem('theme', 'light');
                toggleBtn.innerText = '🌙 Dark Mode';
            } else {
                root.setAttribute('data-theme', 'dark');
                localStorage.setItem('theme', 'dark');
                toggleBtn.innerText = '☀️ Light Mode';
            }
        });
    </script>
</body>
</html>